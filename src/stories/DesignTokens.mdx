import { Meta } from '@storybook/blocks';
import '../../styles/storiesStyle.css';

<Meta title="Design System/Tokens" />

# Design Tokens

디자인 시스템의 기반이 되는 **Design Tokens**입니다. 
모든 컴포넌트에서 일관된 스타일을 사용하기 위한 토큰들을 제공합니다.

## 🎨 Color Palette

### Primary Colors
주요 브랜드 컬러입니다.

<div className="token-display">
  <div className="token-item">
    <div className="token-preview" style={{ backgroundColor: 'var(--color-primary-500)' }}></div>
    <div className="token-name">Primary 500</div>
    <div className="token-value">--color-primary-500</div>
  </div>
  <div className="token-item">
    <div className="token-preview" style={{ backgroundColor: 'var(--color-primary-600)' }}></div>
    <div className="token-name">Primary 600</div>
    <div className="token-value">--color-primary-600</div>
  </div>
  <div className="token-item">
    <div className="token-preview" style={{ backgroundColor: 'var(--color-primary-700)' }}></div>
    <div className="token-name">Primary 700</div>
    <div className="token-value">--color-primary-700</div>
  </div>
</div>

### Semantic Colors
의미적 색상들입니다.

<div className="token-display">
  <div className="token-item">
    <div className="token-preview" style={{ backgroundColor: 'var(--color-success-500)' }}></div>
    <div className="token-name">Success</div>
    <div className="token-value">--color-success-500</div>
  </div>
  <div className="token-item">
    <div className="token-preview" style={{ backgroundColor: 'var(--color-warning-500)' }}></div>
    <div className="token-name">Warning</div>
    <div className="token-value">--color-warning-500</div>
  </div>
  <div className="token-item">
    <div className="token-preview" style={{ backgroundColor: 'var(--color-error-500)' }}></div>
    <div className="token-name">Error</div>
    <div className="token-value">--color-error-500</div>
  </div>
</div>

## ⚡ Easing Functions

애니메이션 이징 함수들입니다. 자세한 내용은 [Easing Tokens](/?path=/docs/design-system-easing-tokens--docs)를 참고하세요.

export const EasingPreview = ({ name, easing }) => (
  <div className="token-item">
    <div className="token-name">{name}</div>
    <div className="token-value" style={{ fontFamily: 'var(--font-family-mono)', fontSize: 'var(--font-size-xs)' }}>
      {easing}
    </div>
  </div>
);

<div className="token-display">
  <EasingPreview name="Standard" easing="cubic-bezier(0.15, 0, 0.15, 1)" />
  <EasingPreview name="Ease Out" easing="cubic-bezier(0.33, 1, 0.68, 1)" />
  <EasingPreview name="Spring" easing="cubic-bezier(0.34, 1.5, 0.54, 1)" />
</div>

---

## 사용법

### CSS에서 사용
```css
.my-component {
  color: var(--text-primary);
  background-color: var(--bg-secondary);
  transition: transform 0.3s var(--ease-spring);
}
```

### JavaScript/TypeScript에서 사용
```javascript
import { setEasing, getEasingCSS } from '@/styles/easing-tokens';

// 배열 형태로 사용
const easing = setEasing('ease_Spring'); // [0.34, 1.5, 0.54, 1]

// CSS 문자열로 사용  
const cssEasing = getEasingCSS('ease_Spring'); // "cubic-bezier(0.34, 1.5, 0.54, 1)"
```

모든 토큰은 CSS Custom Properties로 구현되어 있어 일관성 있는 디자인을 유지할 수 있습니다.