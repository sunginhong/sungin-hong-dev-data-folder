import { Popup } from './Popup.tsx';
import { fn } from '@storybook/test';

export default {
  title: 'Components/Popup',
  component: Popup,
  parameters: {
    layout: 'centered',
  },
 
  argTypes: {
    
  },
};

export const Primary = {
  args: {
   
  },
};